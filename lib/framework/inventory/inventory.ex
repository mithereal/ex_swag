# lib/global_erp/inventory/inventory.ex

defmodule Framework.Inventory do
  alias Framework.Repo

  # Replace with real schema later
  def count_items(_user) do
    # Example: Repo.aggregate(Item, :count, :id)
    124
  end
end
